package spoopy.rendering;

enum SpoopyDrawType {

    /* Specifies a drawing mode where vertex data is defined as an array of individual vertices */
    ARRAY;

    /* Specifies a drawing mode where vertex data is defined as a series of connected vertices */
    ELEMENTS;
}